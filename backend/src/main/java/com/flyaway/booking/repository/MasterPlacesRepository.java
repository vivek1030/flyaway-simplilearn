package com.flyaway.booking.repository;

import com.flyaway.booking.entity.MasterPlaces;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MasterPlacesRepository extends JpaRepository<MasterPlaces, String>{


}
